import { withLayout } from 'src/layouts/layout';
import SuccessPageComponent from 'src/page-component/shop-page-component/success-page-component';

const SuccessPage = () => {
	return <SuccessPageComponent />;
};

export default withLayout(SuccessPage);
